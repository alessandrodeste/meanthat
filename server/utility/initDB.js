
// To db setup:
// node server/utility/initDB.js

// TODO

var config = require('../app/config');
//var initDB = require('../app/initDB');
console.log('*** Configuration: \n', config);
//initDB.initialize(config);
//initDB.addAdminUser(function() {});
